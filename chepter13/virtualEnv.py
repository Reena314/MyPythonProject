'''python3 -m venv venv
source venv/bin/activate
pip install pandas                    for instalation of pandas
'''
# virtual environment which is the same as the interpreter but is isolated from the other python environments on the system

# for installing requirements : pip install -r requirements.txt but first create file pip freeze > requirements.txt

